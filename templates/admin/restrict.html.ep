% layout 'welcome' ;
% my $posts = db->resultset('Post');

% if (flash->{post_saved}) {
    <p>Your post is saved</p>
% }
<div class="container">
<header>
Coal Alley --- management page
</header>
</div>

<div class="container">
% while (my $post = $posts->next) {
    <h2><%= $post->title %></h2>
    <p2><%= $post->date_published %></p2>
    <a href="/edit<%= $post->id %>">edit</a>
    <a href="/delete<%= $post->id %>">delete</a>
% }
</br>
<a href="/add_post">add</a>
</div>
